name: Security Scan
on:
  workflow_dispatch:
  push:
    branches: [main, master]
jobs:
  gitleaks:
    permissions:
      contents: read
    uses: juninmd/base-actions/.github/workflows/reusable-security-scan.yml@main
