Você é dev fullstack no monorepo "Meu Barbeiro" (`api/` Express 5 + Prisma + zod; `web/` React 19 + Vite).

O modelo `Holiday` e os campos `breakStartsAt`/`breakEndsAt` em `BusinessHour` já existem, e as regras de agenda já os respeitam (tarefa anterior). Falta a barbearia conseguir configurá-los.

## Backend

**Almoço** — em `api/src/routes/barbershops.routes.ts`, o `PATCH /current` já valida `businessHours` com `hourSchema`. Estenda para aceitar `breakStartsAt` e `breakEndsAt` opcionais, com as regras:
- ou os dois vêm preenchidos, ou nenhum: meio intervalo não existe;
- `breakStartsAt < breakEndsAt`;
- o intervalo tem que estar dentro de `opensAt`–`closesAt` do mesmo dia;
- dia desabilitado não pode ter almoço.
Mensagens de erro em português, no estilo das que já existem ali. Devolva os campos em `publicBarbershop` para o frontend conseguir editar.

**Feriados** — rotas novas, apenas `OWNER`/`ADMIN` nas escritas, montadas junto das rotas de barbearia:
- `GET /current/holidays` — lista os feriados, do mais próximo para o mais distante. Aceita query opcional `year`.
- `POST /current/holidays` — cria. Recebe `date` (`YYYY-MM-DD`) e `description` (3 a 80). Data repetida responde 409 com mensagem de produto.
- `DELETE /current/holidays/:id` — remove.

Recusar feriado no passado não é necessário: o dono pode estar cadastrando o calendário do ano inteiro.

## Frontend — `web/src/features/barber/BarbershopSettings.tsx`

Este arquivo já é visível apenas para dono/admin; mantenha esse controle.

**Almoço** — na seção de horários de funcionamento, cada dia habilitado ganha os campos de início e fim do almoço, opcionais e desabilitados quando o dia está fechado. Deixe claro na tela que o intervalo bloqueia agendamentos.

**Feriados** — uma seção nova para cadastrar e remover feriados, com data e descrição, listando os já cadastrados. Confirmação antes de remover, reutilizando o `ConfirmDialog` que já existe.

Em `web/src/lib/repository.ts`, implemente também o ramo `mockEnabled` (feriados e almoço no estado mock), para a demonstração continuar completa sem API.

## Frontend — visão do cliente

O expediente exibido ao cliente em `web/src/features/client/ClientDashboard.tsx` já resume os dias e horários. Se o dia tiver almoço, isso precisa aparecer no resumo — o cliente não pode ser surpreendido por um buraco na agenda que ninguém explicou.

## Restrições
- Mudança cirúrgica: não refatore o que não é necessário, não reformate arquivos, não adicione dependência nova.
- Não altere o schema Prisma nem crie migração: o modelo já está pronto.
- Não regrida o que já existe nesses arquivos (visibilidade por papel, botão salvar com estado, diálogo de confirmação, seleção de horários do cliente).
- Siga o estilo do código existente (sem ponto e vírgula, aspas simples, textos em português).

## Testes
- API: validação do almoço (meio intervalo, invertido, fora do expediente, dia fechado) e feriado duplicado.
- Web: ramo mock de feriados em `web/src/lib/repository.test.ts`.
- Testes independentes do fuso da máquina.

## Critério de aceite (rode e conserte até passar)
```
cd api && pnpm exec tsc --noEmit
cd api && pnpm test
cd web && pnpm exec tsc --noEmit
cd web && pnpm lint
cd web && pnpm test
cd web && pnpm build
```
Ao terminar, escreva um resumo do que mudou e o resultado real dos comandos.
